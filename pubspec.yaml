name: preload
description: >-
  Automatically generate preload tags for your Dart web application.
version: 2.0.1-wip
repository: https://github.com/kevmoo/preload

environment:
  sdk: ^3.3.0

dependencies:
  build: ^2.0.0
  # Not imported in code, but used to constrain `build.yaml` requirements
  build_config: ">=1.0.0 <5.0.0"
  glob: ^2.0.0
  path: ^1.8.0

dev_dependencies:
  build_test: ^2.1.0
  dart_flutter_team_lints: ^3.0.0
  logging: ^1.0.0
  test: ^1.16.8
